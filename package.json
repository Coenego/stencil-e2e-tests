{
  "name": "e2etest",
  "private": true,
  "main": "dist/stencil-app.js",
  "scripts": {
    "build": "npm run clean && stencil build --prerender",
    "clean": "rimraf dist",
    "test": "npm run test:e2e",
    "test:e2e": "stencil test --e2e --headless"
  },
  "dependencies": {
    "@stencil/core": "^0.14.0-6"
  },
  "devDependencies": {
    "@types/jest": "^23.3.5",
    "@types/puppeteer": "1.9.0",
    "jest": "^23.6.0",
    "jest-cli": "^23.6.0",
    "puppeteer": "1.8.0",
    "rimraf": "^2.6.2",
    "typescript": "^3.1.3"
  }
}
